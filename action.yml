name: 'PR Risk Scorer'
description: 'Scores pull requests by structural risk (files changed, complexity, coverage, dead code, migrations)'
author: 'FasterApiWeb'

branding:
  icon: 'shield'
  color: 'orange'

inputs:
  github-token:
    description: 'GitHub token for reading PR data'
    required: true
  config-path:
    description: 'Path to pr-risk-scorer.yml config'
    required: false
    default: '.github/pr-risk-scorer.yml'

outputs:
  risk-score:
    description: 'The computed risk score (0-100)'
  risk-label:
    description: 'Risk label: LOW, MEDIUM, or HIGH'

runs:
  using: 'node20'
  main: 'dist/index.js'
